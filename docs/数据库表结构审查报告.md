# AIO Life 数据库表结构审查报告

> 审查对象：`sql/1_init_table/2026-08-18_init_all_tables.sql`（当前基准结构）+ 2026-08-19 ~ 2026-08-22 增量脚本，交叉核对实体类与 Service 写入逻辑。
> 最近复核：2026-08-22。
> 说明：本文档只保留**当前仍待整改**的项与整改记录，不再保留历史结论快照。

---

## 待整改清单

### P1 —— 性能 / 关联正确性

| 问题 | 位置 | 整改 |
|---|---|---|
| `expense` / `income` 缺 `(user_id, 时间)` 索引 | `expense.exp_time`、`income.inc_date` | 补索引（按表时间粒度） |
| `b_video` 缺 `user_id` 索引 | `b_video` | 补索引 |
| `api_key_log` 缺 `create_time` 索引 | `api_key_log`（日志表按时间查询是常态） | 补索引 |
| `exercise_record.exercise_type_id` 字典关联偏离 | varchar(50) 存字典「值」，其他字典消费方用 bigint 主键关联 `user_dict_data.id` | 若引用字典表改 bigint 外键，否则文档注明其性质 |

### P2 —— 一致性 / 维护成本

| 问题 | 位置 | 整改 |
|---|---|---|
| `b_video` 死字段 + 死索引 | `progress`、`last_watched`、`added_at` 实体未映射、代码零引用，`idx_progress` 为死索引；`progress` 与 `watched_duration/duration` 语义重复 | 删列 + 删索引 |
| `goal` 冗余列 | `year/month/day` 与 `start_date/end_date` 重复；`progress` 与 `current_value/target_value` 重复，无 CHECK 约束 | 删冗余列 or 加 CHECK |
| `task_detail.priority` 语义倒置 | 1-高、10-中、20-低，数值与优先级语义相反；`is_starred int(1)` 布尔语义 | 改 1/2/3 单调递增；布尔统一 `tinyint(1)` |
| `honor_record.level` varchar 存数值 | varchar(20) 存「1-校级~5-国际级」 | 纯枚举改 `tinyint` + 注释 |
| `read_record` / `movie.total_progress` 语义双关 | 注释「总页数或 100 表示百分比」 | 明确语义或拆列 |
| `expense.amt` / `transaction_amt` 注释含糊 | 记账金额 vs 支付金额仅靠实体注释区分 | 同步库注释 |
| `time_record.create_user` 注释误标 | 审计列注释写「用户ID」，与真实归属列 `user_id` 混淆 | 改注释「创建人」 |
| `thought.content` 注释错别字 | 注释写「没人」 | 改「内容」 |
| `income.inc_date` 命名/粒度不一致 | 同域 `expense.exp_time` 为 datetime，`inc_date` 为 date 命名孤立 | 统一命名与粒度 |
| `exercise_record.exercise_count` 无注释 | — | 补注释 |
| `enum_type` 例外表规则未文档化 | 仍用 `created_at/updated_at` 且无 `is_deleted` | 在表结构约定中注明例外 |
| 唯一索引删除后的业务兜底确认 | `user_quick_nav`、`notification_preference`、`system_config.config_key`、`api_key` 等唯一索引已于 `2026-08-19_drop_logic_delete_unique_indexes.sql` 删除 | 逐一确认业务层有「有效记录唯一」兜底 |

---

## 已整改记录（仅存根，不展开）

| 整改 | 落点 |
|---|---|
| P0 `device` 日期 varchar→date；逻辑删除+唯一索引冲突（8 处删唯一索引）；审计字段统一 `create_user/update_user` bigint | `2026-08-19_*.sql`（drop_logic_delete_unique_indexes / unify_audit_columns / complete_business_audit_columns / fix_device_date_columns） |
| P2 `performance` 审计字段统一 | `2026-08-21_fix_performance_audit_columns.sql` |
| P1 `task.column_id` / `expense.exp_type_id` / `income.inc_type_id` int→bigint；对齐目标表主键 `task_column.id` / `user_dict_data.id` | `2026-08-22_align_fk_column_types.sql` + 全量建表脚本同步 |
| P1 `time_record.category_id` varchar(32)→bigint（三步幂等：放宽 NOT NULL→清洗非法值→MODIFY） | `2026-08-22_align_time_record_category_id.sql` + 实体/Mapper/Service/Controller 链路同步 |
| P2 `income` 主键 `income_id`→`id`（对齐 `expense.id`） | `2026-08-22_unify_income_primary_key.sql` + `IncomeEntity` 同步 |
| 类型口径 | 本轮相关列统一为 `bigint`（不写长度，MySQL 8 已弃用显示宽度） |

---

## 已决策不回改

- `login_log.password` 明文存密码 —— **有意保留**：登录失败时记录明文密码，用于抓取弱密码/常见密码样本构建密码本（既非缺陷也非疏漏，属刻意设计）。注释已写清于建表脚本 / `docs/数据库表结构.md` / `LoginLogEntity` / `UserServiceImpl.login()`，勿删勿改。
- `time_record.id` 保持 varchar(32)（前端 UUID 方案）、`relate_id` 保持 bigint 外链、`start_time/end_time` smallint 分钟表示 —— 既定设计，不在整改范围。

---

## 注意事项

- 存量库需**手工执行**增量脚本（新库走全量建表脚本，不需重复执行）。`align_time_record_category_id` 有诊断 SELECT 可预估受影响行数。
- 所有 `ALTER` 执行前先评估数据迁移（类型变更、唯一索引重建），避免线上数据截断或插入失败。